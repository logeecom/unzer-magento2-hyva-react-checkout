# Installation

Open the package.json file and add the following line:

```json
"config": {
  "paymentMethodsRepo": {
    "unzer": "git@github.com:logeecom/unzer-magento2-hyva-react-checkout.git"
  }
}
```

Next, run `npm install`. This should install the repository into your checkout.
